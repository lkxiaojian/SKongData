<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="voiceListViewModel"
            type="com.zky.multi_media.mvvm.viewmodle.VoiceListViewModle" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:focusableInTouchMode="true">

        <androidx.appcompat.widget.AppCompatEditText
            android:id="@+id/art_search"
            android:layout_width="269dp"
            android:layout_height="30dp"
            android:layout_marginStart="12dp"
            android:layout_marginTop="10dp"
            android:background="@drawable/shape_fff_3"
            android:drawableStart="@mipmap/voice_search"
            android:drawablePadding="6dp"
            android:hint="按名称搜索"
            android:text="@={voiceListViewModel.searchMessage}"
            android:textColor="@color/color_9b9b9b"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/abt_search"
            android:layout_width="55dp"
            android:layout_height="30dp"
            android:layout_marginTop="10dp"
            android:layout_marginEnd="12dp"
            android:background="@drawable/button_4a90e2_selector"
            android:focusable="true"
            android:onClick="@{voiceListViewModel::startClick}"
            android:text="搜索"
            android:textColor="@color/white"
            android:textSize="18sp"
            android:textStyle="bold"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"

            />


        <com.refresh.lib.DaisyRefreshLayout
            android:id="@+id/drl_voice"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="50dp"
            android:layout_marginBottom="54dp"
            app:enableLoadMore="@{safeUnbox(voiceListViewModel.enableLoadMore)}"
            app:enableRefresh="@{safeUnbox(voiceListViewModel.enableRefresh)}"
            app:layout_constraintTop_toBottomOf="@+id/art_search"
            app:onAutoRefreshCommand="@{voiceListViewModel.onAutoRefreshCommand}"
            app:onLoadMoreCommand="@{voiceListViewModel.onLoadMoreCommand}"
            app:onRefreshCommand="@{voiceListViewModel.onRefreshCommand}">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recview"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@color/color_f5f5f5"
                app:linearLayoutManager="@{safeUnbox(voiceListViewModel.orientation)}" />
        </com.refresh.lib.DaisyRefreshLayout>


        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/abt_sure"
            android:layout_width="match_parent"
            android:layout_height="34dp"
            android:layout_marginStart="12dp"
            android:layout_marginEnd="12dp"
            android:layout_marginBottom="20dp"
            android:background="@drawable/button_4a90e2_selector"
            android:onClick="@{voiceListViewModel::startClick}"
            android:text="确        认"
            android:textColor="@color/white"
            android:textSize="14sp"
            android:textStyle="bold"
            app:layout_constraintBottom_toBottomOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>